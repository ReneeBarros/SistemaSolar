
spring:
  datasource:
    url: jdbc:h2:file:./solarOffGrid
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    format_sql: true


  h2:
    console:
      enabled: true
  server:
    port: ${SERVER_PORT:8080}